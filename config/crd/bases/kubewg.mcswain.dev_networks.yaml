---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: networks.kubewg.mcswain.dev
spec:
  group: kubewg.mcswain.dev
  names:
    kind: Network
    listKind: NetworkList
    plural: networks
    singular: network
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.router[?(@.replicas > 0)]
      name: Routed
      type: boolean
    - jsonPath: .spec.firewall.enabled
      name: Firewalled
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Network is the Schema for the networks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkSpec defines the desired state of Network
            properties:
              firewall:
                description: FirewallSpec defines the firewall configuration for a
                  container
                properties:
                  allowWorkloadNetworkChanges:
                    type: boolean
                  egress:
                    items:
                      description: FirewallRulesSpec defines a list of firewall rules
                      properties:
                        allow:
                          items:
                            description: FirewallRuleSpec defines a firewall rule
                            properties:
                              cidr:
                                type: string
                              endPort:
                                type: integer
                              ip:
                                type: string
                              protocol:
                                description: Protocol defines a network protocol
                                enum:
                                - UDP
                                - TCP
                                - ICMP
                                - ALL
                                type: string
                              startPort:
                                type: integer
                            required:
                            - protocol
                            - startPort
                            type: object
                          type: array
                        block:
                          items:
                            description: FirewallRuleSpec defines a firewall rule
                            properties:
                              cidr:
                                type: string
                              endPort:
                                type: integer
                              ip:
                                type: string
                              protocol:
                                description: Protocol defines a network protocol
                                enum:
                                - UDP
                                - TCP
                                - ICMP
                                - ALL
                                type: string
                              startPort:
                                type: integer
                            required:
                            - protocol
                            - startPort
                            type: object
                          type: array
                      type: object
                    type: array
                  enabled:
                    type: boolean
                  ingress:
                    items:
                      description: FirewallRulesSpec defines a list of firewall rules
                      properties:
                        allow:
                          items:
                            description: FirewallRuleSpec defines a firewall rule
                            properties:
                              cidr:
                                type: string
                              endPort:
                                type: integer
                              ip:
                                type: string
                              protocol:
                                description: Protocol defines a network protocol
                                enum:
                                - UDP
                                - TCP
                                - ICMP
                                - ALL
                                type: string
                              startPort:
                                type: integer
                            required:
                            - protocol
                            - startPort
                            type: object
                          type: array
                        block:
                          items:
                            description: FirewallRuleSpec defines a firewall rule
                            properties:
                              cidr:
                                type: string
                              endPort:
                                type: integer
                              ip:
                                type: string
                              protocol:
                                description: Protocol defines a network protocol
                                enum:
                                - UDP
                                - TCP
                                - ICMP
                                - ALL
                                type: string
                              startPort:
                                type: integer
                            required:
                            - protocol
                            - startPort
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              init:
                description: InitSpec defines the initial container configuration
                properties:
                  dns:
                    description: DNSSpec defines the DNS configuration for a container
                    properties:
                      nameservers:
                        items:
                          type: string
                        type: array
                    type: object
                  image:
                    type: string
                type: object
              router:
                description: |-
                  INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                properties:
                  externalVPN:
                    description: ExternalVPNSpec defines the an external VPN connection
                    properties:
                      connection:
                        description: WireguardConnectionSpec defines a Wireguard connection
                        properties:
                          address:
                            type: string
                          port:
                            type: integer
                          secret:
                            description: NameSelectorSpec defines a name selector
                              for a resource
                            properties:
                              name:
                                type: string
                            required:
                            - name
                            type: object
                        type: object
                      credentials:
                        description: WireguardCredentialsSpec defines a set of Wireguard
                          credentials
                        properties:
                          peerPublicKey:
                            description: PeerPublicKey is the 44-character public
                              key for the peer in base64 format
                            maxLength: 44
                            minLength: 44
                            type: string
                          preSharedKey:
                            description: PreSharedKey is the optional pre-shared key
                              for the Wireguard connection
                            type: string
                          privateKey:
                            description: PrivateKey is the 44-character private key
                              for the Wireguard client in base64 format
                            maxLength: 44
                            minLength: 44
                            type: string
                          secret:
                            description: Secret is the name of the secret containing
                              the Wireguard credentials in the keys "privateKey",
                              "peerPublicKey", and "preSharedKey"
                            properties:
                              name:
                                type: string
                            required:
                            - name
                            type: object
                        type: object
                    required:
                    - connection
                    - credentials
                    type: object
                  image:
                    type: string
                  replicas:
                    format: int32
                    type: integer
                type: object
            type: object
          status:
            description: NetworkStatus defines the observed state of Network
            properties:
              id:
                type: string
              ready:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: boolean
              replicas:
                format: int32
                type: integer
              selector:
                type: string
              status:
                type: integer
            required:
            - ready
            - replicas
            - selector
            type: object
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.router.replicas
        statusReplicasPath: .status.replicas
      status: {}
